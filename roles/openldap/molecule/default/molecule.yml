---
# Molecule test - syntax validation only

platforms: []

scenario:
  test_sequence:
    - syntax
provisioner:
  inventory:
    group_vars:
      all:
        openldap_enabled: true
        openldap_data_dir: "/tmp/openldap"
        openldap_admin_password: "testpassword123"
        openldap_config_password: "testpassword123"
