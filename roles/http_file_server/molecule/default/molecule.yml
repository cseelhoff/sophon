---
# Molecule test - syntax validation only

platforms: []

scenario:
  test_sequence:
    - syntax
provisioner:
  inventory:
    group_vars:
      all:
        http_file_server_port: 8000
        http_file_server_dir: "/tmp/http_files"
        file_to_serve: "test-file.txt"
        server_port: 8000
