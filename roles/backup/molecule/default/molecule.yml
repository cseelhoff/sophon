---
# Molecule test - syntax validation only

platforms: []

scenario:
  test_sequence:
    - syntax
provisioner:
  inventory:
    group_vars:
      all:
        backup_enabled: true
        backup_data_dir: "/tmp/backup"
        backup_local_path: "/tmp/backup-dest"
