---
# =============================================================================
# Proxmox Connection Settings
# =============================================================================
# These are fallback defaults - override via inventory group_vars or -e flags

proxmox_host: ""
proxmox_username: "root"
proxmox_user: "{{ proxmox_username }}@pam"
proxmox_password: ""
proxmox_node: "proxmox"
proxmox_port: 8006
proxmox_disk_storage_id: "local-lvm"
proxmox_airgapped: false

# Certificate validation (set to true in production with valid certs)
proxmox_validate_certs: false

# Proxmox storage for the CoreOS image
proxmox_iso_storage_id: "local"
proxmox_iso_storage_path: "/var/lib/vz/template/iso"
