---
# Molecule test - syntax validation only

platforms: []

scenario:
  test_sequence:
    - syntax
provisioner:
  inventory:
    group_vars:
      all:
        coredns_enabled: true
        coredns_data_dir: "/tmp/coredns"
